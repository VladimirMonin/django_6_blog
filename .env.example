# Copy to .env for local development if your shell/editor loads it.
# The Django app itself reads process environment variables.

DJANGO_SECRET_KEY=change-this-local-development-key
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,testserver
