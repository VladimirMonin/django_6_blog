# Фаза 4: Гибридный семантический поиск

## Общая цель

Создание интеллектуальной системы поиска по контенту блога, способной находить релевантную информацию не только по ключевым словам, но и по смыслу запроса. Система должна "понимать" структуру технического контента: различать код, текст, таблицы и диаграммы, обрабатывая каждый тип оптимальным способом.

В отличие от классического полнотекстового поиска, семантический поиск находит статьи по концептуальной близости: запрос "как настроить подключение к базе" найдёт статью про `DATABASES` в Django, даже если эти слова не встречаются в тексте. Гибридный подход (векторный + лексический поиск через RRF) обеспечивает лучшее из двух миров.

---

## Результат фазы

| Функция | Описание |
|---------|----------|
| **PostgreSQL + pgvector** | Миграция с SQLite на PostgreSQL 17+ с расширением pgvector 0.8+ для хранения векторов |
| **Модель TextChunk** | Атомарная единица знаний с типизацией (TEXT/CODE/MERMAID/IMAGE_DESC) и привязкой к секциям |
| **Сервис чанкинга** | Маршрутизируемый пайплайн: код → `CodeChunker` (AST), текст → `SemanticChunker` |
| **Сервис векторизации** | Batch-запросы к OpenAI API с кэшированием через `content_hash` |
| **Гибридный поиск** | RRF-алгоритм для объединения семантического и лексического поиска |
| **HTMX UI поиска** | Интерактивный интерфейс с "хлебными крошками" контекста и расширением результатов |

---

## Философия фазы

> **"Понимание структуры, а не поиск по строкам"**

### ✅ Что делаем

- Миграция на PostgreSQL — единое хранилище для реляционных и векторных данных
- Типизированные чанки — каждый фрагмент "знает" свой тип (код/текст/диаграмма)
- Контекстная привязка — чанк хранит путь "Статья → Секция H2 → Позиция"
- Кэширование через `content_hash` — экономия на API-вызовах при неизменённом контенте
- Manual Control — никаких "магических" абстракций, полный контроль над логикой

### ❌ Чего не делаем

- Не используем внешние векторные БД (Pinecone, Weaviate) — всё в PostgreSQL
- Не строим Knowledge Graph — это задача Phase 5+
- Не реализуем Agentic RAG с LLM — только поиск без генерации ответов
- Не внедряем Cross-Encoder Reranking — это оптимизация "Day 2"

---

## Подфазы

### Группа 1: Инфраструктура БД

#### [Фаза 4.1](phase_4.1.md): Миграция на PostgreSQL

Отказ от SQLite в пользу PostgreSQL 17+ с Docker-контейнером для разработки.

**Результат:** Проект работает на PostgreSQL, данные мигрированы через `dumpdata`/`loaddata`.

**Для архитектурного сериала:** Демонстрация Docker Compose конфигурации, переключение `settings.py`, миграция данных.

**Документация:** [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) — раздел 3 "Фундамент данных: PostgreSQL 18.1"

---

#### [Фаза 4.2](phase_4.2.md): Настройка pgvector и FTS

Установка расширений `pgvector`, `ltree`. Настройка русского FTS (лемматизация).

**Результат:** Расширения активны, `CREATE EXTENSION pgvector` выполнен, русский словарь настроен.

**Для архитектурного сериала:** Показать SQL-команды активации расширений, проверку работы FTS на русском тексте.

**Документация:** [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) — раздел 4 "Векторный движок: pgvector 0.8.0+"

---

### Группа 2: Модели данных

#### [Фаза 4.3](phase_4.3.md): Модель TextChunk

Создание модели `TextChunk` — атомарной единицы знаний с векторным полем, типизацией и контекстной привязкой.

**Результат:** Модель с полями `content`, `embedding` (halfvec), `chunk_type`, `section_title`, `position_index`, `content_hash`. Индексы HNSW созданы.

**Для архитектурного сериала:** Показать структуру модели, объяснить каждое поле и его роль в поиске.

**Документация:** [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — раздел "Расширенная концептуальная модель данных"

---

#### [Фаза 4.4](phase_4.4.md): Связь Post → TextChunk

Реализация FK от `TextChunk` к `Post` с каскадным удалением. Сигнал `post_save` для триггера индексации.

**Результат:** При сохранении поста запускается задача переиндексации (пока синхронно).

**Для архитектурного сериала:** Демонстрация Django Signals, каскадного удаления чанков при удалении поста.

---

### Группа 3: Сервисы обработки

#### [Фаза 4.5](phase_4.5.md): Сервис извлечения структуры

Парсинг Markdown для выделения секций (H2/H3) и блоков кода. Использование существующего `MarkdownProcessor` как основы.

**Результат:** Функция `extract_sections(markdown_text)` возвращает список секций с заголовками и контентом, разделённым на потоки CODE/TEXT.

**Для архитектурного сериала:** Показать как HTML-процессоры проекта переиспользуются для структурного парсинга.

**Документация:** [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — "ШАГ A: Структурный Парсинг"

---

#### [Фаза 4.6](phase_4.6.md): Сервис чанкинга (Chonkie)

Интеграция библиотеки Chonkie: `SemanticChunker` для текста, `CodeChunker` (AST через tree-sitter) для кода.

**Результат:** Функция `chunk_content(sections)` возвращает типизированные чанки с метаданными.

**Для архитектурного сериала:** Демонстрация работы разных чанкеров на примере статьи с кодом и текстом.

**Документация:**

- [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) — раздел 2 "Слой ингестии: Chonkie"
- [Django, pgvector: семантический поиск локально](../researches/report_6/Django,%20pgvector_%20семантический%20поиск%20локально.md) — раздел 2 "Эволюция инструментов локального чанкинга"

---

#### [Фаза 4.7](phase_4.7.md): Сервис векторизации (OpenAI API)

Batch-запросы к OpenAI `text-embedding-3-small`. Кэширование через `content_hash`, retry с exponential backoff.

**Результат:** Функция `vectorize_chunks(chunks)` обогащает чанки эмбеддингами, пропуская неизменённые.

**Для архитектурного сериала:** Показать экономику API: подсчёт токенов через tiktoken, демонстрация кэширования.

**Документация:** [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — "ШАГ C: Оркестрация векторизации"

---

#### [Фаза 4.8](phase_4.8.md): Пайплайн индексации

Объединение сервисов в единый пайплайн: `Post.save()` → `extract_sections()` → `chunk_content()` → `vectorize_chunks()` → `save_chunks()`.

**Результат:** Management-команда `python manage.py index_posts` для массовой индексации. Хук в `post_save` для инкрементальной.

**Для архитектурного сериала:** End-to-end демонстрация индексации статьи от Markdown до векторов в БД.

---

### Группа 4: Поисковый движок

#### [Фаза 4.9](phase_4.9.md): Семантический поиск

Реализация векторного поиска через pgvector: косинусное расстояние (`<=>`), HNSW индекс.

**Результат:** Функция `semantic_search(query, limit=10)` возвращает топ-N семантически близких чанков.

**Для архитектурного сериала:** SQL-запрос с оператором `<=>`, объяснение HNSW графа.

**Документация:** [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) — раздел 4.4 "Индексация HNSW"

---

#### [Фаза 4.10](phase_4.10.md): Лексический поиск (FTS)

Реализация полнотекстового поиска через PostgreSQL FTS: `to_tsvector`, `plainto_tsquery`, ранжирование.

**Результат:** Функция `lexical_search(query, limit=10)` возвращает топ-N по точному совпадению терминов.

**Для архитектурного сериала:** Демонстрация работы русского стеммера, поиск технических терминов.

---

#### [Фаза 4.11](phase_4.11.md): Гибридный поиск (RRF)

Объединение семантического и лексического поиска через Reciprocal Rank Fusion.

**Результат:** Функция `hybrid_search(query, limit=10)` с формулой `Score = 1/(60+rank_vec) + 1/(60+rank_fts)`.

**Для архитектурного сериала:** Визуализация RRF: два ранжированных списка → объединённый результат.

**Документация:**

- [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) — раздел 5.3 "Алгоритм RRF"
- [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — "ЭТАП 3: Поисковый движок"

---

### Группа 5: UI и UX

#### [Фаза 4.12](phase_4.12.md): API поиска (View + URL)

Django View для обработки поисковых запросов. Возврат JSON или HTML-partial для HTMX.

**Результат:** Эндпоинт `/search/` с параметром `q`, поддержка HTMX-запросов.

**Для архитектурного сериала:** Интеграция с django-htmx, определение типа запроса.

---

#### [Фаза 4.13](phase_4.13.md): Компонент результатов поиска

Django-component `search_result` с подсветкой кода (Pygments), "хлебными крошками" контекста.

**Результат:** Переиспользуемый компонент для отображения чанка с метаданными и ссылкой на пост.

**Для архитектурного сериала:** Демонстрация компонентного подхода, цветовая кодировка типов чанков.

**Документация:** [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — "UX/UI паттерны (HTMX & Frontend)"

---

#### [Фаза 4.14](phase_4.14.md): HTMX-интерфейс поиска

Страница поиска с live-результатами, debounce, кнопкой "Показать контекст" для расширения чанка.

**Результат:** Полноценный UI поиска с мгновенным откликом и возможностью "раскрыть" соседние чанки.

**Для архитектурного сериала:** Демонстрация UX: ввод запроса → мгновенные результаты → клик "Развернуть".

---

### Группа 6: Админка и инструменты

#### [Фаза 4.15](phase_4.15.md): Расширение Unfold для индексации

Кастомные Actions в админке: "♻️ Force Re-Index", "💰 Cost Estimator" (подсчёт стоимости через tiktoken).

**Результат:** Администратор может переиндексировать выбранные посты и оценить стоимость операции.

**Для архитектурного сериала:** Демонстрация интеграции с Unfold, UX админки.

**Документация:** [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) — "ЭТАП 4: Управление и DX"

---

#### [Фаза 4.16](phase_4.16.md): Визуализатор чанков

Inline-виджет в админке поста для отображения результата нарезки с цветовой кодировкой типов.

**Результат:** Администратор видит как статья разбилась на чанки, может выявить проблемы нарезки.

**Для архитектурного сериала:** Демонстрация дебаг-инструмента, выявление "разорванной функции".

---

## Технологический стек

### Новые зависимости

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `psycopg[binary]` | 3.x | PostgreSQL драйвер для Django |
| `pgvector` | 0.3+ | Django-интеграция для pgvector |
| `chonkie[semantic]` | 1.0.4+ | Семантический чанкинг |
| `tiktoken` | latest | Подсчёт токенов для OpenAI |
| `openai` | 1.x | API клиент для эмбеддингов |

### Встроенные модули

- `hashlib` — генерация `content_hash` (SHA256)
- `json` — метаданные чанков

### Что сохраняем

- `django-components` — компонент результатов поиска
- `django-htmx` — интерактивный UI
- `unfold` — расширение админки
- `pymdownx` — обработка Markdown (переиспользуем для парсинга)

---

## Архитектура решения

### Пайплайн индексации

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────────┐
│  Post.save()│────▶│ extract_sections │────▶│  chunk_content  │
│  (signal)   │     │   (Markdown)     │     │   (Chonkie)     │
└─────────────┘     └──────────────────┘     └─────────────────┘
                                                      │
                                                      ▼
┌─────────────┐     ┌──────────────────┐     ┌─────────────────┐
│ PostgreSQL  │◀────│   save_chunks    │◀────│vectorize_chunks │
│  pgvector   │     │   (bulk_create)  │     │  (OpenAI API)   │
└─────────────┘     └──────────────────┘     └─────────────────┘
```

### Маршрутизация контента

```
┌─────────────────────────────────────────────────────────┐
│                    Секция (H2/H3)                       │
├─────────────────┬───────────────────┬───────────────────┤
│   Блок кода     │    Текст          │    Таблица        │
│   ```python     │    Абзацы,        │    Markdown       │
│   ...           │    списки         │    table          │
│   ```           │                   │                   │
├─────────────────┼───────────────────┼───────────────────┤
│  CodeChunker    │ SemanticChunker   │  TableChunker     │
│  (AST/tree-     │ (косинус между    │  (строки +        │
│   sitter)       │  предложениями)   │   заголовки)      │
└─────────────────┴───────────────────┴───────────────────┘
                          │
                          ▼
              ┌─────────────────────┐
              │    TextChunk        │
              │  chunk_type = ...   │
              │  embedding = [...]  │
              └─────────────────────┘
```

### Гибридный поиск (RRF)

```
                    Запрос пользователя
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
    ┌─────────────────┐       ┌─────────────────┐
    │ Семантический   │       │  Лексический    │
    │ поиск (pgvector)│       │  поиск (FTS)    │
    │ embedding <=>   │       │  to_tsvector    │
    └────────┬────────┘       └────────┬────────┘
             │ rank_vec                │ rank_fts
             └────────────┬────────────┘
                          ▼
              ┌─────────────────────┐
              │   RRF Fusion        │
              │ 1/(60+r_vec) +      │
              │ 1/(60+r_fts)        │
              └─────────────────────┘
                          │
                          ▼
              ┌─────────────────────┐
              │   Топ-N результатов │
              │   + контекст        │
              └─────────────────────┘
```

---

## Модели данных

### TextChunk

Атомарная единица знаний, связанная с постом.

**Поля:**

- `post` — FK на Post (CASCADE), обязательно
- `section_title` — заголовок секции H2/H3, в которой находится чанк (для "хлебных крошек")
- `content` — текст или код чанка (отображается в результатах)
- `chunk_type` — ENUM: TEXT, CODE, MERMAID_DESC, IMAGE_DESC
- `code_language` — язык программирования для CODE (nullable), для подсветки синтаксиса
- `position_index` — порядковый номер в статье (для загрузки соседей N-1, N+1)
- `content_hash` — SHA256 хеш контента (для идемпотентности, экономии API)
- `embedding` — halfvec(1536) для OpenAI text-embedding-3-small
- `search_vector` — tsvector для FTS (автообновляется триггером)

**Индексы:**

- HNSW на `embedding` (vector_cosine_ops, m=16, ef_construction=64)
- GIN на `search_vector` (для FTS)
- B-Tree на `content_hash` (для проверки дубликатов)
- Composite на (`post`, `position_index`) (для загрузки контекста)

---

## Безопасность

| Риск | Защита |
|------|--------|
| Утечка API-ключа OpenAI | Хранение в `.env`, использование `django-environ` |
| SQL-инъекции в raw queries | Параметризованные запросы для RRF |
| DoS через поисковые запросы | Rate limiting на уровне View (django-ratelimit) |
| Чрезмерные расходы на API | Cost Estimator в админке, лимиты на batch size |

---

## Совместимость с Phase 3

### Что остаётся без изменений

- Модель `Post` — добавляется только `related_name='chunks'` через FK в TextChunk
- Все компоненты (`post_card`, `alert`, `button`, `paginator`)
- Сервисы `markdown_converter`, `html_processor` — переиспользуются
- Шаблоны `post_list`, `post_detail`

### Что изменяется

- `config/settings.py` — переключение на PostgreSQL
- `Post.save()` — добавляется сигнал для индексации
- Админка Post — добавляется inline для чанков и actions

### Зависимости от Phase 3

⚠️ **Рекомендуется завершить Phase 3.1-3.6** (Category, Tag, PostMedia) до начала Phase 4, так как:

- Метаданные Category/Tag могут использоваться для фильтрации поиска
- PostMedia влияет на обработку IMAGE_DESC чанков
- Структура Post стабилизируется после Phase 3

---

## Риски и митигация

| Риск | Вероятность | Причина | Митигация |
|------|-------------|---------|-----------|
| Высокая стоимость OpenAI API | 🟡 Средняя | Большой архив постов | `content_hash` кэширование, Cost Estimator, локальные модели для dev |
| FTS для русского работает плохо | 🟡 Средняя | Стандартный стеммер неточен | hunspell словарь или pg_trgm для fuzzy search |
| tree-sitter не парсит псевдокод | 🟢 Низкая | Код в блоге часто неполный | Fallback на текстовый чанкинг с логированием |
| HNSW индекс не помещается в RAM | 🟢 Низкая | Мало данных на старте | halfvec (2x экономия), мониторинг pg_stat_io |
| Celery overhead для мелких задач | 🟡 Средняя | Синхронная индексация блокирует | Начинаем синхронно, Celery добавляем в Phase 4+ |

---

## Успех фазы

- ✅ PostgreSQL 17+ работает в Docker, данные мигрированы
- ✅ pgvector и ltree расширения активны
- ✅ Модель TextChunk создана с HNSW индексом
- ✅ Management-команда `index_posts` индексирует все посты
- ✅ Гибридный поиск возвращает релевантные результаты
- ✅ UI поиска работает с HTMX, показывает "хлебные крошки"
- ✅ Админка позволяет Force Re-Index и показывает Cost Estimate
- ✅ Поиск по коду возвращает целые функции, а не обрывки

---

## Следующая фаза

### Phase 5: Расширенный поиск и мультимодальность

Возможные направления:

- **Mermaid Diagrams** — LLM-описания диаграмм для векторизации
- **Image Search** — Vision API для генерации описаний изображений
- **Cross-Encoder Reranking** — переранжирование топ-N для повышения precision
- **Celery для индексации** — асинхронная обработка больших объёмов
- **pgvectorscale** — масштабирование на SSD (DiskANN)

---

## Чеклист готовности

Перед началом Phase 4 убедитесь:

- [ ] Phase 3.1-3.6 завершены (Category, Tag, PostMedia)
- [ ] Docker установлен для PostgreSQL контейнера
- [ ] OpenAI API ключ получен и добавлен в `.env`
- [ ] Изучены документы из `doc/researches/report_6/`
- [ ] Текущие данные SQLite экспортированы через `dumpdata`

---

## Время выполнения

| Группа | Подфазы | Оценка |
|--------|---------|--------|
| Инфраструктура БД | 4.1 - 4.2 | 3-4 часа |
| Модели данных | 4.3 - 4.4 | 2-3 часа |
| Сервисы обработки | 4.5 - 4.8 | 8-10 часов |
| Поисковый движок | 4.9 - 4.11 | 4-5 часов |
| UI и UX | 4.12 - 4.14 | 4-5 часов |
| Админка | 4.15 - 4.16 | 2-3 часа |
| **Итого** | 16 подфаз | **~24-30 часов** |

---

## Ключевые документы исследования

Все технические решения Phase 4 основаны на документах из `doc/researches/report_6/`:

| Документ | Основной фокус |
|----------|----------------|
| [ИИ поиск на базе смыслов — концепция](../researches/report_6/ИИ%20поиск%20на%20базе%20смыслов%20-%20концепция.md) | Архитектурный blueprint, философия, дорожная карта, модель Chunk |
| [Технический отчёт: Chonkie, PostgreSQL, pgvector](../researches/report_6/Технический%20отчет_%20Chonkie,%20Postgresql,%20pgvector.md) | PostgreSQL 18.1, pgvector 0.8, Chonkie стратегии, HNSW, RRF |
| [Django, pgvector: семантический поиск локально](../researches/report_6/Django,%20pgvector_%20семантический%20поиск%20локально.md) | Django ORM интеграция, сравнение чанкеров, паттерны кода |
| [Техническая спецификация: Универсальная контент-платформа](../researches/report_6/Техническая%20спецификация_%20Универсальная%20контент-платформа%20MD%20посты%20Блог.md) | Post-centric архитектура, статусы, медиа (для контекста Phase 3) |
