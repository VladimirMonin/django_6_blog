# Фаза 2.1: Установка и настройка Unfold админки

## Цель

Заменить стандартную Django админку на современный интерфейс django-unfold с профессиональным дизайном и улучшенным UX.

## Контекст

Django-unfold — это современная альтернатива стандартной админке Django с:

- **Красивым интерфейсом** — темная тема, плавные анимации, Tabler Icons
- **Responsive дизайном** — работает на мобильных устройствах
- **Расширенными виджетами** — Monaco Editor, DateTime pickers, загрузка файлов
- **Производительностью** — оптимизированные запросы, ленивая загрузка

**Философия:** Unfold сохраняет всю функциональность Django admin, только улучшает UI/UX.

**Документация:** <https://unfoldadmin.com/>

## Задачи

### 1. Установка django-unfold

- [ ] Установить django-unfold через Poetry:

  ```bash
  poetry add django-unfold
  ```

- [ ] Проверить установленную версию:

  ```bash
  poetry show django-unfold
  ```

- [ ] Убедиться, что версия >= 0.30.0 (поддержка Django 6)

### 2. Базовая настройка в settings.py

- [ ] Заменить `django.contrib.admin` на `unfold` и `unfold.contrib.filters.admin` в `INSTALLED_APPS`:

  ```python
  INSTALLED_APPS = [
      "unfold",  # Должен быть ПЕРЕД django.contrib.admin
      "unfold.contrib.filters.admin",  # Опционально, но рекомендуется
      "django.contrib.admin",
      "django.contrib.auth",
      # ...
  ]
  ```

- [ ] Добавить Unfold в `TEMPLATES` loaders (для кастомных шаблонов):

  ```python
  TEMPLATES = [
      {
          "OPTIONS": {
              "loaders": [
                  (
                      "django.template.loaders.cached.Loader",
                      [
                          "django.template.loaders.filesystem.Loader",
                          "unfold.loaders.UnfoldLoader",  # Добавить
                          "django.template.loaders.app_directories.Loader",
                          "django_components.template_loader.Loader",
                      ],
                  ),
              ],
          },
      },
  ]
  ```

### 3. Конфигурация Unfold под дизайн проекта

- [ ] Добавить настройки `UNFOLD` в `settings.py` (в конце файла):

  ```python
  UNFOLD = {
      "SITE_TITLE": "Django 6 Blog",
      "SITE_HEADER": "Админ-панель блога",
      "SITE_URL": "/",
      "SITE_ICON": {
          "light": "/static/icon-light.svg",  # Опционально
          "dark": "/static/icon-dark.svg",    # Опционально
      },
      "SITE_LOGO": None,  # Или путь к логотипу
      "SITE_SYMBOL": "speed",  # Tabler icon (для демонстрации)
      "SHOW_HISTORY": True,
      "SHOW_VIEW_ON_SITE": True,
      "ENVIRONMENT": "blog.utils.environment_callback",  # Опционально
      "COLORS": {
          "primary": {
              "50": "255 248 225",   # Светло-желтый (accent)
              "100": "255 236 179",
              "200": "255 224 130",
              "300": "255 213 79",
              "400": "255 202 40",
              "500": "255 193 7",    # Желтый акцент (#FFC107)
              "600": "255 179 0",
              "700": "255 160 0",
              "800": "255 143 0",
              "900": "230 81 0",
              "950": "26 13 0",
          },
      },
      "EXTENSIONS": {
          "modeltranslation": {
              "flags": {
                  "en": "🇬🇧",
                  "ru": "🇷🇺",
              },
          },
      },
      "SIDEBAR": {
          "show_search": True,
          "show_all_applications": True,
          "navigation": [
              {
                  "title": "Навигация",
                  "separator": True,
                  "collapsible": True,
                  "items": [
                      {
                          "title": "Главная",
                          "icon": "home",
                          "link": "/",
                      },
                      {
                          "title": "Админка",
                          "icon": "dashboard",
                          "link": "/admin/",
                      },
                  ],
              },
              {
                  "title": "Блог",
                  "separator": True,
                  "collapsible": True,
                  "items": [
                      {
                          "title": "Посты",
                          "icon": "article",
                          "link": "/admin/blog/post/",
                      },
                  ],
              },
          ],
      },
      "TABS": [
          {
              "models": [
                  "blog.post",
              ],
              "items": [
                  {
                      "title": "Основная информация",
                      "link": lambda obj: f"/admin/blog/post/{obj.pk}/change/",
                  },
              ],
          },
      ],
  }
  ```

### 4. Обновление админки Post модели

- [ ] Открыть `blog/admin.py`
- [ ] Импортировать классы из Unfold:

  ```python
  from unfold.admin import ModelAdmin
  from unfold.decorators import display
  ```

- [ ] Обновить `PostAdmin` для наследования от `unfold.admin.ModelAdmin`:

  ```python
  @admin.register(Post)
  class PostAdmin(ModelAdmin):
      # Все существующие настройки остаются
      list_display = ("title", "slug", "created_at", "is_published")
      list_filter = ("is_published", "created_at")
      search_fields = ("title", "content")
      prepopulated_fields = {"slug": ("title",)}
      date_hierarchy = "created_at"
      ordering = ("-created_at",)

      fieldsets = (
          ("Основная информация", {"fields": ("title", "slug", "content")}),
          ("Статус", {"fields": ("is_published",)}),
          ("Даты", {
              "fields": ("created_at", "updated_at"),
              "classes": ("collapse",)
          }),
      )

      readonly_fields = ("created_at", "updated_at")
  ```

- [ ] Добавить кастомные методы с декораторами Unfold (опционально):

  ```python
  @display(description="Статус", boolean=True)
  def display_published(self, obj):
      return obj.is_published

  # Обновить list_display:
  # list_display = (..., "display_published")
  ```

### 5. Создание суперпользователя (если нет)

- [ ] Проверить, есть ли суперпользователь:

  ```bash
  python manage.py shell
  >>> from django.contrib.auth.models import User
  >>> User.objects.filter(is_superuser=True).exists()
  ```

- [ ] Если нет, создать:

  ```bash
  python manage.py createsuperuser
  ```

### 6. Проверка работоспособности

- [ ] Запустить сервер:

  ```bash
  python manage.py runserver
  ```

- [ ] Открыть админку: <http://127.0.0.1:8000/admin/>
- [ ] Убедиться, что:
  - Отображается Unfold интерфейс (темная тема, новые иконки)
  - Боковая панель с навигацией работает
  - Модель Post отображается корректно
  - Все фильтры, поиск, fieldsets работают
  - Желтый акцент применяется (кнопки, ссылки)

### 7. Тестирование основного функционала

- [ ] Создать новый пост через админку:
  - Проверить prepopulated_fields для slug
  - Проверить DateTime pickers
  - Проверить сохранение
- [ ] Отредактировать существующий пост:
  - Проверить readonly_fields
  - Проверить fieldsets (collapse работает)
- [ ] Использовать поиск в админке:
  - Поиск по заголовку
  - Поиск по контенту
- [ ] Применить фильтры:
  - Фильтр по is_published
  - Фильтр по created_at (date_hierarchy)

### 8. Кастомизация (опционально)

- [ ] Добавить custom actions в `PostAdmin`:

  ```python
  @admin.action(description="Опубликовать выбранные посты")
  def publish_posts(self, request, queryset):
      queryset.update(is_published=True)

  @admin.action(description="Снять с публикации")
  def unpublish_posts(self, request, queryset):
      queryset.update(is_published=False)

  actions = [publish_posts, unpublish_posts]
  ```

- [ ] Настроить компактное отображение в list_display (более сжатый UI):

  ```python
  list_display = (
      "title",
      "slug",
      "display_published",
      "display_created_at",
  )

  @display(description="Дата создания")
  def display_created_at(self, obj):
      return obj.created_at.strftime("%d.%m.%Y %H:%M")
  ```

### 9. Добавление favicon для админки (опционально)

- [ ] Создать или скачать favicon.ico
- [ ] Поместить в `static/` папку
- [ ] Добавить в `UNFOLD` настройки:

  ```python
  "SITE_ICON": {
      "light": "/static/favicon.ico",
      "dark": "/static/favicon.ico",
  },
  ```

## Результат

Полностью функциональная Unfold админка с:

- Темной темой и желтыми акцентами
- Сворачиваемой боковой панелью
- Кастомной навигацией
- Всеми возможностями стандартной админки
- Улучшенным UX/UI

## Документация

- [Unfold официальная документация](https://unfoldadmin.com/)
- [Unfold GitHub](https://github.com/unfoldadmin/django-unfold)
- [Tabler Icons](https://tabler-icons.io/) — для выбора иконок

## Следующий шаг

Фаза 2.2: Полная русификация приложения

## Возможные проблемы и решения

### Проблема 1: Unfold не отображается

**Симптомы:** Остается стандартная админка после установки.

**Решение:**

- Проверить порядок в `INSTALLED_APPS` (unfold должен быть ПЕРЕД admin)
- Очистить кэш браузера (Ctrl+Shift+R)
- Перезапустить сервер

### Проблема 2: Статические файлы не загружаются

**Симптомы:** Админка без стилей, только HTML.

**Решение:**

```bash
python manage.py collectstatic --clear --noinput
```

Для разработки убедиться, что `DEBUG = True` и `STATICFILES_DIRS` настроены.

### Проблема 3: Конфликт с django-components loaders

**Симптомы:** Ошибка импорта шаблонов компонентов.

**Решение:**

Порядок loaders в `TEMPLATES`:

1. `filesystem.Loader`
2. `unfold.loaders.UnfoldLoader`
3. `app_directories.Loader`
4. `django_components.template_loader.Loader`

### Проблема 4: Желтый акцент не применяется

**Симптомы:** Кнопки остаются дефолтного цвета Unfold (синие).

**Решение:**

- Убедиться, что настройки `UNFOLD["COLORS"]["primary"]` добавлены
- Перезагрузить страницу с очисткой кэша
- Проверить, что цвета указаны в формате RGB (без `#`)

## Дополнительные возможности Unfold

### Виджеты (для будущих фаз)

- **Monaco Editor** — для Markdown контента (Phase 2.3)
- **WYSIWYG Editor** — альтернатива (если откажемся от Markdown)
- **Improved FileField** — для загрузки изображений (Phase 3+)
- **InlineModelAdmin** — для комментариев (Phase 3+)

### Фильтры

- **RangeNumericFilter** — для фильтрации по диапазону
- **RangeDateFilter** — для дат
- **SliderNumericFilter** — слайдер для чисел
- **MultipleChoiceFilter** — множественный выбор

### Кастомизация Dashboard

Unfold позволяет создавать кастомные дашборды с:

- Графиками (через Chart.js)
- Статистикой (количество постов, просмотры)
- Быстрыми действиями (создать пост, модерация)

**Оставляем на будущее:** Phase 3+ когда появится больше моделей.
