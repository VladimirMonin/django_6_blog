# **🏗️ PROJECT CANVAS: Exception Blog — AI Search Engine 2.0 (Extended Architecture)**

Версия архитектуры: 1.2 (Deep Hybrid RAG \+ Structural AST)  
Стек: Django 6, PostgreSQL (pgvector), Chonkie (Manual Mode), OpenAI/Gemini API, HTMX  
Статус: Blueprint / Ready for Implementation

## **🎯 1\. Концепция и Философия Системы**

Мы создаем не просто блог с функцией поиска, а **Интеллектуальную Базу Знаний (Knowledge Graph-ready)**, ориентированную на технический контент. В то время как стандартные RAG-системы воспринимают документ как плоский текст, наша система должна "понимать" анатомию технической статьи: иерархию заголовков, синтаксическую целостность кода и семантические связи между концепциями.  
**Фундаментальные принципы архитектуры:**

1. **Code Integrity (Целостность Кода):** Мы объявляем войну "разорванным функциям". Использование AST (Abstract Syntax Tree) гарантирует, что блок кода всегда будет проиндексирован как логически завершенная единица (функция, класс, конфигурационный блок), а не как случайный набор строк, ограниченный лимитом токенов.  
2. **Structural Awareness (Структурная Осведомленность):** Поиск не существует в вакууме. Любой найденный фрагмент жестко привязан к своему контексту: "Статья \-\> Раздел (H2) \-\> Подраздел (H3)". Это позволяет пользователю мгновенно понять, к какой части системы относится найденный ответ.  
3. **Hybrid Retrieval (Гибридный Поиск):** Мы признаем, что векторы не идеальны. Векторный поиск находит смыслы ("как настроить базу"), а лексический поиск (FTS) находит точные совпадения (DATABASES \= {...}). Мы объединяем эти миры через алгоритм **RRF**, чтобы получить лучшее из обоих подходов.  
4. **Manual Control (Ручное Управление):** Мы отказываемся от "магических" абстракций библиотек, которые скрывают логику. Мы используем библиотеки (как Chonkie) только как инструменты (utility), но бизнес-логику нарезки, встраивания и сохранения реализуем самостоятельно внутри Django.

## **🗺️ ДЕТАЛЬНАЯ ДОРОЖНАЯ КАРТА (ROADMAP)**

### **🧱 ЭТАП 1: Инфраструктурный Фундамент (The Base)**

*Цель: Создать надежный монолитный слой хранения данных, способный эффективно выполнять роль как реляционной БД, так и векторного движка.*

1. **Миграция и Настройка PostgreSQL:**  
   * **Обоснование:** SQLite с расширением sqlite-vec отлично подходит для прототипирования, но для production-ready системы с гибридным поиском PostgreSQL является стандартом де\-факто. Он обеспечивает ACID-транзакции, надежную репликацию и богатую экосистему расширений.  
   * **Действие:** Полный отказ от SQLite в пользу PostgreSQL 16+.  
   * **Векторный Движок:** Активация и тюнинг расширения **pgvector**.  
     * *Стратегия индексации:* На старте проекта (до 100k векторов) можно использовать индекс IVFFlat (Inverted File Flat) для экономии оперативной памяти. По мере роста базы, для сохранения низкой латентности поиска (\<50ms), необходимо мигрировать на индекс HNSW (Hierarchical Navigable Small World), который обеспечивает лучшую производительность ценой большего потребления RAM.  
   * **Полнотекстовый Движок (FTS):** Конфигурация встроенного FTS движка PostgreSQL.  
     * *Лемматизация:* Подключение и настройка словарей (русский/английский) hunspell или встроенных стеммеров. Это критически важно, чтобы поиск по слову "настройка" находил документы со словами "настройки", "настроить", "настроил". Без этого качество лексического поиска будет низким.  
2. **Глубокое Моделирование Данных (Schema Design):**  
   * **Центральная Сущность:** Проектирование таблицы Chunk как атомарной единицы знаний. Она не должна быть перегружена лишними данными, но обязана содержать все необходимое для ранжирования и отображения.  
   * **Контекстная Привязка:** Реализация механизма **"Хлебных крошек"** на уровне модели. Это может быть реализовано через поле path (материализованный путь, например, /setup/database/pgvector) или через жесткую связь (Foreign Key) с иерархической моделью Section. Это гарантирует, что каждый чанк "знает" свое место в структуре документа.  
   * **Идемпотентность:** Внедрение поля chunk\_hash (MD5 или SHA256 от контента чанка). Это поле служит уникальным ключом для дедупликации. Перед отправкой текста на платное API векторизации, система проверяет наличие хеша в базе. Если текст не изменился, мы экономим деньги и время, пропуская этап векторизации.

### **⚙️ ЭТАП 2: Движок Индексации "Сэндвич" (Ingestion Engine)**

*Цель: Реализовать интеллектуальный конвейер обработки, который превращает сырой Markdown в структурированные знания, сохраняя семантическую целостность.*  
**Детальная Логика Пайплайна (Backend Process):**  
**ШАГ A: Структурный Парсинг (Custom Logic)**

* **Вход:** "Сырой" Markdown файл всей статьи целиком.  
* **Действие:** Специализированный парсер (реализованный на базе mistune или оптимизированных регулярных выражений) сканирует документ, игнорируя мелкие детали, и ищет структуру верхнего уровня — заголовки H2.  
* **Логика "Контекстного Контейнера":** Весь контент, находящийся между двумя заголовками H2, логически объединяется в сущность **Секция**. Секция — это единица смысла верхнего уровня (например, "Установка", "Конфигурация", "Примеры"). Секция наследует заголовок и глобальные метаданные поста (автор, дата, теги).  
* *Результат:* Список объектов "Секция", очищенных от глобального шума и готовых к гранулярной нарезке.

ШАГ B: Маршрутизация и Умная Нарезка (Chonkie Integration)  
Внутри каждой Секции запускаем дифференцированную обработку контента, разделяя потоки данных:

* **Поток КОДА (Code Pipeline):**  
  * **Детекция:** Выделение блоков кода, обрамленных тройными апострофами \`\`\`.  
  * **Идентификация:** Определение языка программирования по тегу (например, python, javascript, bash).  
  * **AST-Chunking:** Передача контента в **AST-Chunker** (из библиотеки Chonkie). Этот компонент строит абстрактное синтаксическое дерево кода. Он "знает", где начинается и заканчивается функция или класс.  
  * *Специфика (Fail-safe):* Если парсер встречает синтаксическую ошибку (код в блоге часто бывает неполным или псевдокодом), он не должен вызывать исключение. Должна сработать fallback-стратегия: нарезка кода как обычного текста или по строкам, с сохранением предупреждения в логах.  
* **Поток ТЕКСТА (Text Pipeline):**  
  * **Обработка:** Весь текст, не являющийся кодом, передается в **Semantic/Late Chunker**.  
  * *Специфика:* Использование подхода "Late Chunking" (если модель эмбеддинга это поддерживает). В этом режиме модель сначала "читает" весь текст секции, создавая контекстно-зависимые векторы для каждого токена, и только потом происходит разделение на чанки. Это решает проблему "потерянных местоимений", когда чанк начинается со слов "Он это сделал", и без контекста непонятно, кто "он" и что "это".

**ШАГ C: Оркестрация Векторизации**

* **Агрегация:** Сбор всех нарезанных и классифицированных чанков (код и текст) в единый буфер обработки.  
* **Smart Batching:** Группировка чанков в пакеты (batch) оптимального размера для выбранного API. Например, для OpenAI это может быть 20-50 чанков за раз. Это критически важно для производительности, так как минимизирует накладные расходы на сетевые соединения (Network Overhead / Handshake).  
* **Robustness (Отказоустойчивость):** Реализация логики повторных попыток (Retry with Exponential Backoff). Если API возвращает ошибку 429 (Rate Limit) или 503 (Service Unavailable), система должна ждать (например, 1с, 2с, 4с) и повторять попытку, а не падать с ошибкой.

### **🧠 ЭТАП 3: Поисковый Движок (Retrieval Engine)**

*Цель: Реализовать математически обоснованную логику ранжирования, которая устойчива к разным типам формулировок запросов и обеспечивает высокую релевантность.*  
**Алгоритм Обработки Запроса:**

1. **Preprocessing (Предобработка):**  
   * Нормализация запроса пользователя: удаление лишних пробелов, спецсимволов, исправление очевидных опечаток (опционально, с помощью простых эвристик или легковесных библиотек).  
   * Генерация векторного представления запроса через то же самое API и с теми же параметрами, что использовались при индексации.  
2. **Parallel Execution (Параллельный Поиск):**  
   * Система запускает два независимых поиска одновременно (или последовательно в рамках одной транзакции):  
   * **Ветвь 1: Семантический поиск (pgvector):**  
     * Выполняется SQL-запрос с использованием оператора косинусного расстояния (\<=\>).  
     * Цель: Найти концептуальные совпадения, синонимы, связанные понятия.  
   * **Ветвь 2: Лексический поиск (Postgres FTS):**  
     * Выполняется SQL-запрос websearch\_to\_tsquery к полю tsvector.  
     * Цель: Найти точные вхождения технических терминов, имен конкретных переменных, специфических кодов ошибок или названий библиотек, которые векторная модель могла "размыть".  
3. **Reciprocal Rank Fusion (RRF):**  
   * Объединение результатов происходит не через простое сложение скоров (так как шкалы косинусного расстояния и bm25 разные), а через ранги.  
   * Применение формулы: Score \= 1 / (k \+ Rank\_Vector) \+ 1 / (k \+ Rank\_FTS).  
   * *Значение:* Эта формула позволяет "всплыть" наверх результатам, которые релевантны в обоих методах (консенсус), и эффективно штрафует результаты, которые случайно попали в топ только одного метода. Константа сглаживания k (обычно 60\) предотвращает доминирование одного из методов при малом количестве результатов.  
4. **Context Assembly (Сборка Контекста):**  
   * После формирования финального списка топ-N чанков, система выполняет пост-обработку.  
   * Проверяется поле position\_index. Если найденный чанк является, например, вторым шагом в инструкции из 5 шагов, система может автоматически подгрузить чанк N-1 и N+1. Это позволяет отобразить пользователю не вырванный из контекста кусок, а связный блок информации.

### **🎛️ ЭТАП 4: Управление и DX (Admin Experience)**

*Цель: Превратить Django Admin из простого CRUD-интерфейса в мощный пульт управления поисковой системой.*

1. **Расширение Интерфейса (Unfold):**  
   * Создание кастомных Actions для массовой обработки статей в списке (changelist).  
   * **"♻️ Force Re-Index":** Действие для принудительного удаления всех существующих чанков выбранной статьи и запуска пайплайна индексации с нуля. Это необходимо при изменении логики нарезки, исправлении багов в парсере или смене модели эмбеддинга.  
   * **"💰 Cost Estimator":** Утилитарная функция, которая подсчитывает количество токенов (через tiktoken) во всех выбранных статьях и показывает примерную стоимость их полной переиндексации в долларах, основываясь на актуальных тарифах API.  
2. **Инструменты Отладки (Debugging Tools):**  
   * **Chunk Visualizer:** Специальная вкладка (TabularInline или кастомный виджет) в админке поста, которая визуализирует результат нарезки. Использование цветовой кодировки (например, зеленый для кода, синий для текста) помогает мгновенно понять качество работы сплиттера: "Ага, вот тут AST-сплиттер ошибся и разорвал предложение, нужно править парсер".  
   * **Manual Override:** Возможность для администратора вручную отредактировать границы чанка (текст) или его метаданные в базе данных. Это "аварийный люк" на случай, если автоматика не справилась со сложным форматированием, позволяющий исправить поиск без необходимости менять исходный Markdown файл.

### **🔮 ЭТАП 5: Мультимодальное Масштабирование**

*Цель: Расширение возможностей поиска за пределы текста, превращение системы в мультимедийную базу знаний.*

1. **Mermaid Diagrams Support:**  
   * Парсер обучается обнаруживать блоки кода с языком mermaid.  
   * Вместо того чтобы сохранять код диаграммы (который семантически беден для векторного поиска), система отправляет его в дешевую LLM с промптом: *"Опиши эту диаграмму текстом. Кто с кем взаимодействует? Какие процессы и потоки данных изображены?"*.  
   * Полученное текстовое описание векторизуется и сохраняется. Теперь пользователь может найти диаграмму по запросу "схема авторизации OAuth", даже если в самом коде диаграммы эти слова явно не встречаются.  
2. **Image & Media Search:**  
   * Аналогичный подход применяется для изображений. Использование Vision-моделей (например, GPT-4o-mini или локальных аналогов) для генерации подробных Alt-текстов (Image Captioning).  
   * Векторизация этих описаний позволяет пользователям искать скриншоты интерфейсов, архитектурные схемы и фотографии по их смысловому содержанию, а не только по подписям, сделанным вручную.

## **💽 РАСШИРЕННАЯ КОНЦЕПТУАЛЬНАЯ МОДЕЛЬ ДАННЫХ**

Описание структуры базы данных с обоснованием каждого поля и его роли в системе.  
**Сущность: Chunk (Чанк)**

* **parent\_post (ForeignKey):** Жесткая ссылка на статью. Обеспечивает целостность данных: при удалении статьи (cascade delete) автоматически удаляются все её чанки и векторы.  
* **section\_title (Char):** Заголовок секции (H2/H3), к которой принадлежит чанк. Используется в UI для отображения контекста в результатах поиска ("Найдено в разделе: Конфигурация").  
* **content (Text):** Непосредственно текст или код чанка. Именно это поле показывается пользователю в сниппете результата.  
* **content\_vector (Vector):** Эмбеддинг, полученный от API (OpenAI/Gemini). Размерность фиксируется на уровне схемы БД (1536 или 768). Индексируется алгоритмом HNSW для быстрого поиска.  
* **content\_search (TsVector):** Специальное поле для лексического поиска (FTS). Автоматически обновляется триггером базы данных или пересчитывается при сохранении модели при изменении поля content.  
* **chunk\_type (Enum):**  
  * TEXT: Обычный текст (абзацы, списки).  
  * CODE: Блок кода.  
  * MERMAID\_DESC: Текстовое описание диаграммы.  
  * IMAGE\_DESC: Текстовое описание (caption) изображения.  
* **code\_language (Char, nullable):** Указывает язык программирования (python, js, html) для чанков типа CODE. Критически важно для правильной подсветки синтаксиса (Syntax Highlighting) на фронтенде.  
* **content\_hash (Char):** MD5 или SHA хеш от исходного текста чанка. Служит ключом идемпотентности для предотвращения дублирования работы API.  
* **position\_index (Int):** Порядковый номер чанка внутри статьи. Критически важно для функции "Показать в контексте" (загрузить соседей N-1 и N+1).

## **💡 UX/UI ПАТТЕРНЫ (HTMX & Frontend)**

1. **Streaming Results:** Поиск — операция потенциально долгая. Интерфейс не должен блокироваться. Используем HTMX для показа индикатора загрузки и постепенного появления результатов по мере их готовности (или сразу блоком после завершения запроса).  
2. **Code Highlighting:** Найденные чанки типа CODE должны рендериться с применением server-side подсветки (библиотеки Pygments или Rouge) или client-side (Highlight.js/Prism.js), чтобы пользователь сразу видел читаемый синтаксис, а не просто моноширинный текст.  
3. **"Zoom Out" Interaction (Расширение контекста):**  
   * Пользователь видит найденный чанк (например, 3 строки кода, вырванные из середины функции).  
   * Нажимает кнопку "Развернуть" или "Показать контекст".  
   * HTMX делает запрос к бэкенду, передавая ID текущего чанка.  
   * Бэкенд находит чанки с position\_index \-1 и \+1, рендерит их HTML и вставляет в DOM до и после текущего элемента, создавая эффект бесшовного расширения статьи прямо в результатах поиска.