# План исследования: Python-Markdown для Django 6

## Цель исследования

Изучить библиотеку Python-Markdown и способы её интеграции с Django 6 для обработки и отображения markdown-контента в блоге.

## Приоритет использования инструментов

1. **ПЕРВАЯ ОЧЕРЕДЬ**: Использовать инструмент **Context7** для получения документации Python-Markdown
2. **ВТОРАЯ ОЧЕРЕДЬ**: Использовать **веб-поиск** для примеров интеграции с Django и best practices

## Ключевые вопросы для исследования

### 1. Основы Python-Markdown

- Что такое Python-Markdown и его возможности
- Установка и базовая настройка
- Поддерживаемый синтаксис Markdown
- Версия библиотеки - актуальная стабильная

### 2. Интеграция с Django 6

- Способы интеграции Python-Markdown в Django
- Использование в моделях (сохранение markdown и HTML)
- Template tags и filters для рендеринга markdown
- Готовые пакеты: `django-markdown`, `django-markdownx`, другие

### 3. Расширения (Extensions)

- Встроенные расширения Python-Markdown:
  - Tables (таблицы)
  - Fenced Code Blocks (блоки кода с подсветкой)
  - Table of Contents (оглавление)
  - Footnotes (сноски)
  - Abbreviations (аббревиатуры)
  - Attribute Lists (атрибуты для элементов)
- Сторонние расширения
- Создание собственных расширений

### 4. Подсветка синтаксиса кода

- Интеграция с Pygments для подсветки кода
- Настройка цветовых схем
- Поддержка различных языков программирования
- Frontend библиотеки: highlight.js, Prism.js

### 5. Безопасность

- Sanitization HTML (очистка от опасного контента)
- Защита от XSS атак
- Разрешенные HTML теги
- Использование bleach или html5lib

### 6. Работа с изображениями и медиа

- Загрузка и хранение изображений
- Пути к изображениям в markdown
- Оптимизация изображений
- Lazy loading и responsive images

### 7. Редактор для админки

- WYSIWYG vs сырой markdown
- django-markdownx - редактор с preview
- SimpleMDE, EasyMDE - frontend редакторы
- Live preview в админке Django

### 8. Производительность

- Кэширование отрендеренного HTML
- Pre-rendering vs on-the-fly rendering
- Сохранение HTML в БД vs рендеринг при запросе
- Оптимизация для больших документов

### 9. SEO и метаданные

- Извлечение метаданных из markdown (front matter)
- Генерация meta descriptions
- Структурированные данные для статей
- Open Graph теги

## Инструкции для агента

### Шаг 1: Поиск через Context7

```text
1. Найти документацию Python-Markdown:
   - Использовать resolve-library-id для "markdown" или "python-markdown"
   - Получить документацию по темам:
     * "installation"
     * "extensions"
     * "using-as-module"
     * "reference"
     * "security"

2. Найти документацию для Django-пакетов:
   - Использовать resolve-library-id для "django-markdown" или "django-markdownx"
   - Получить информацию об интеграции
```

### Шаг 2: Дополнительный веб-поиск

```text
Запросы для веб-поиска:
- "Python-Markdown Django 6 integration"
- "django-markdownx tutorial"
- "Django markdown blog tutorial"
- "Python-Markdown extensions guide"
- "Django markdown security best practices"
- "Django markdown editor admin"
- "Pygments Django integration"
- "Django markdown caching"
```

## Ожидаемые результаты

### Документация должна содержать

1. **Установка и настройка** Python-Markdown для Django 6
2. **Базовый пример** интеграции
3. **Архитектурные решения**:
   - Где хранить markdown (модель, файлы)
   - Когда рендерить (сохранение, запрос, кэш)
   - Как организовать редактирование
4. **Настройка расширений** с примерами
5. **Безопасность** - как защититься от XSS
6. **Редактор для админки** - варианты и настройка
7. **Практические примеры** для блога

### Структура результирующего документа

```markdown
# Python-Markdown в Django 6 Blog

## Введение
[Зачем markdown в блоге]

## Установка и настройка
[Python-Markdown + дополнительные пакеты]

## Архитектура решения
[Модели, рендеринг, хранение]

## Базовая интеграция

### Вариант 1: Template Filter
[Код + объяснение]

### Вариант 2: Сохранение HTML в БД
[Код + объяснение]

### Вариант 3: Django-Markdownx
[Готовое решение]

## Модель для блог-поста
[Пример модели с markdown полем]

## Расширения Python-Markdown

### Таблицы
[Синтаксис и настройка]

### Блоки кода с подсветкой
[Pygments интеграция]

### Оглавление (TOC)
[Автогенерация навигации]

### Другие расширения
[Сноски, аббревиатуры, и т.д.]

## Редактор в админке
[django-markdownx или альтернативы]

## Безопасность
[Sanitization и защита от XSS]

## Работа с изображениями
[Upload, storage, отображение]

## Кэширование
[Стратегии кэширования HTML]

## SEO оптимизация
[Метаданные, descriptions, structured data]

## Практический пример: Блог-пост
[Полный код от модели до template]

## Best Practices
[Рекомендации и паттерны]

## Troubleshooting
[Частые проблемы и решения]

## Источники
[Документация и ресурсы]
```

## Практические примеры для реализации

Агент должен найти/создать примеры:

1. **Модель BlogPost** с полем для markdown
2. **Template filter** для рендеринга markdown
3. **Admin форма** с markdown редактором
4. **Блок кода** с подсветкой синтаксиса
5. **Таблица** в markdown
6. **TOC (Table of Contents)** для длинной статьи
7. **Изображение** в markdown посте
8. **Sanitization** опасного HTML
9. **Кэширование** отрендеренного контента
10. **Извлечение метаданных** для SEO

## Архитектурные решения для исследования

### 1. Хранение контента

**Вариант A**: Только markdown в БД

```python
class Post(models.Model):
    content = models.TextField()  # markdown
    # Рендеринг при каждом запросе
```

**Вариант B**: Markdown + кэшированный HTML

```python
class Post(models.Model):
    content_markdown = models.TextField()
    content_html = models.TextField(blank=True)  # кэш
    # Рендеринг при сохранении
```

**Вариант C**: Только HTML (рендеринг перед сохранением)

```python
class Post(models.Model):
    content = models.TextField()  # HTML
    # Markdown только в форме редактирования
```

### 2. Место рендеринга

- В модели (метод или property)
- В view
- В template (через filter)
- В форме (при сохранении)

### 3. Редактор

- SimpleMDE (JavaScript)
- django-markdownx (Django пакет)
- Custom решение
- Без preview (чистый textarea)

## Особое внимание

### Критические аспекты

- ⚠️ **ПРИОРИТЕТ**: Безопасность - sanitization и XSS защита
- ⚠️ **ПРИОРИТЕТ**: Подсветка кода - настройка Pygments
- ⚠️ **ПРИОРИТЕТ**: Редактор в админке - UX для авторов
- 🔍 Производительность - когда рендерить
- 🔍 Изображения - upload и storage
- 🔍 SEO - метаданные из markdown

## Критерии полноты исследования

- ✅ Установка и базовая настройка описана
- ✅ Архитектурное решение выбрано и обосновано
- ✅ Модель для блог-поста создана
- ✅ Расширения настроены (code, tables, TOC)
- ✅ Подсветка синтаксиса работает
- ✅ Редактор для админки настроен
- ✅ Безопасность обеспечена (sanitization)
- ✅ Работа с изображениями решена
- ✅ Кэширование настроено
- ✅ SEO оптимизация реализована

## Связь с другими исследованиями

Это исследование связано с:

- **plan-2**: Инновации в шаблонах Django 6 (рендеринг markdown в templates)
- **plan-3**: Django Components (компоненты для markdown контента)
- **plan-4**: HTMX (live preview markdown с HTMX)

## Специфика для блога

Особые требования для блог-проекта:

- Syntax highlighting для примеров кода
- Оглавление для длинных статей
- Footnotes для ссылок и пояснений
- Responsive изображения
- Code blocks с копированием кода
- Dark/Light theme для code highlighting

## Альтернативы для сравнения

- Markdown vs reStructuredText
- Python-Markdown vs mistune
- Python-Markdown vs markdown2
- WYSIWYG редакторы (TinyMCE, CKEditor)

---

**Дата создания плана**: 26 ноября 2025  
**Проект**: Django 6 Blog  
**Статус**: Готов к исследованию
