# 📱 Серия 19: Мобильная адаптивность

> Медиа-запросы для breadcrumbs, code блоков и таблиц

**Коммиты:** `9203464`, `878d880`

---

## 📌 Проблема: Desktop UI на мобильных

**На десктопе** все выглядит отлично:
- Breadcrumbs с H2 и H3  
- Code блоки с горизонтальным скроллом  
- Таблицы растягиваются на весь экран

**На мобильных (≤768px):**  
❌ Breadcrumbs занимают 2 строки (мало места)  
❌ Code блоки выходят за границы экрана  
❌ Таблицы растягивают страницу (горизонтальный скролл всей страницы)

---

## 🎯 Решение: Медиа-запросы

Используем **CSS media queries** для адаптации:

```css
@media (max-width: 768px) {
    /* Правила только для экранов ≤768px */
}
```

**Что адаптируем:**

1. Breadcrumbs (скрываем H3, упрощаем dropdown)
2. Code блоки (переносим строки)
3. Таблицы (горизонтальный скролл внутри контейнера)

---

## 🧭 Breadcrumbs: Скрываем H3

### Проблема

**На мобильном:**

```
┌─────────────────────────────┐
│ Модульный JavaScript /      │
│ Интеграция с base.html      │ ← 2 строки!
└─────────────────────────────┘
```

**Breadcrumbs занимают слишком много вертикального пространства.**

---

### Решение: Показываем только H2

```css
@media (max-width: 768px) {
    .breadcrumb-item.active {
        display: none; /* Скрываем H3 (active = последний элемент) */
    }
    
    .breadcrumb-separator:last-of-type {
        display: none; /* Скрываем последний " / " */
    }
}
```

**Результат:**

```
┌─────────────────────────────┐
│ Модульный JavaScript        │ ← 1 строка
└─────────────────────────────┘
```

---

### Упрощаем dropdown

**На мобильном:**
- Уменьшаем шрифт: `0.85rem` → `0.8rem`  
- Уменьшаем размер: `min-width: 200px` (было 250px)  
- Ограничиваем высоту: `max-height: 300px` (было 400px)

```css
@media (max-width: 768px) {
    .breadcrumbs-dynamic {
        font-size: 0.8rem;
        padding: 0.5rem 0;
    }
    
    .breadcrumb-dropdown-menu {
        min-width: 200px;
        max-width: 90vw; /* Не шире экрана */
        max-height: 300px;
        font-size: 0.85rem;
    }
    
    .breadcrumb-dropdown-item {
        padding: 0.4rem 0.75rem; /* Было 0.5rem 1rem */
    }
}
```

**Сравнение:**

| Параметр | Desktop | Mobile |
|----------|---------|--------|
| Шрифт breadcrumbs | 0.9rem | 0.8rem |
| Dropdown min-width | 250px | 200px |
| Dropdown max-height | 400px | 300px |
| Padding items | 0.5rem 1rem | 0.4rem 0.75rem |

---

## 💻 Code блоки: Перенос строк

### Проблема

**Длинная строка кода на мобильном:**

```python
def convert_markdown_to_html(markdown_path, template, result_folder):
    # Код уходит за пределы экрана →→→→→→→→→
```

Пользователь должен **скроллить влево-вправо** внутри `<pre>` блока.

---

### Решение: white-space: pre-wrap

```css
@media (max-width: 768px) {
    .post-content pre {
        white-space: pre-wrap;
        word-wrap: break-word;
        overflow-x: auto; /* На случай очень длинных слов */
    }
    
    .post-content pre code {
        white-space: pre-wrap;
        word-break: break-word;
    }
}
```

**Что делает `white-space: pre-wrap`?**

| Значение | Пробелы | Переносы строк | Перенос текста |
|----------|---------|----------------|----------------|
| `pre` | Сохраняет | Сохраняет | ❌ Нет |
| `pre-wrap` | Сохраняет | Сохраняет | ✅ Есть |

**До:**

```
┌────────────────────────┐
│ def convert():         │
│     very_long_function_→→→
└────────────────────────┘
  ← нужно скроллить →
```

**После:**

```
┌────────────────────────┐
│ def convert():         │
│     very_long_function_│
│     name_that_wraps()  │
└────────────────────────┘
```

---

### Почему не всегда?

**На десктопе** `white-space: pre` **лучше:**  
- Код выглядит как в редакторе  
- Горизонтальный скролл привычен программистам  
- Не ломается форматирование

**На мобильных** `white-space: pre-wrap` **необходим:**  
- Скроллить влево-вправо пальцем неудобно  
- Маленький экран → видна маленькая часть кода  
- Лучше перенести строку, чем скроллить

---

## 📊 Таблицы: table-responsive обертка

### Проблема

**Широкая таблица на мобильном:**

```
| Библиотека | Описание | Версия | Лицензия |
|------------|----------|--------|----------|
| Django     | Web ...  | 6.0    | BSD      |
```

Таблица **шире экрана** → страница растягивается → горизонтальный скролл **всей страницы**.

---

### Решение: Bootstrap table-responsive

**В Python коде** (`TableProcessor`) оборачиваем таблицы:

```python
def process(self, soup: BeautifulSoup) -> None:
    for table in soup.find_all("table"):
        # Добавляем Bootstrap классы
        table["class"] = ["table", "table-striped", "table-hover", "table-bordered"]
        
        # Оборачиваем в div.table-responsive
        wrapper = soup.new_tag("div")
        wrapper["class"] = ["table-responsive"]
        table.wrap(wrapper)
```

**Что получается в HTML:**

```html
<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered">
        <thead>...</thead>
        <tbody>...</tbody>
    </table>
</div>
```

**Bootstrap CSS делает свою магию:**

```css
.table-responsive {
    display: block;
    width: 100%;
    overflow-x: auto; /* Скролл только внутри контейнера */
    -webkit-overflow-scrolling: touch; /* Плавный скролл на iOS */
}
```

---

### Результат

**До:**

```
┌───────────────────────────────────────→→→→
│ | Библиотека | Описание | Версия | ...  →→→
│ |------------|----------|--------|------→→→
│ | Django     | Framework| 6.0    | BSD  →→→
└───────────────────────────────────────→→→→
  ← Вся страница скроллится →
```

**После:**

```
┌────────────────────────┐
│ [Таблица]              │
│ ├────────────────────→→│ ← Скролл только внутри
│ │ Django | 6.0 | BSD →→│
│ └────────────────────→→│
└────────────────────────┘
```

Остальной контент **не двигается**, скроллится только таблица.

---

### Идемпотентность

**Проблема:** Если запустить процессор дважды, таблица обернется **дважды**:

```html
<div class="table-responsive">
    <div class="table-responsive">
        <table>...</table>
    </div>
</div>
```

**Решение:** Проверяем перед оберткой:

```python
if table.parent and table.parent.name == "div":
    parent_classes = table.parent.get("class", [])
    if "table-responsive" in parent_classes:
        continue  # Уже обернута, пропускаем
```

---

## 📊 Сравнение: Desktop vs Mobile

### Breadcrumbs

| Параметр | Desktop (>768px) | Mobile (≤768px) |
|----------|------------------|-----------------|
| **H2 видим** | ✅ Да | ✅ Да |
| **H3 видим** | ✅ Да | ❌ Нет (скрыт) |
| **Dropdown размер** | 250-400px | 200px (max 90vw) |
| **Шрифт** | 0.9rem | 0.8rem |

### Code блоки

| Параметр | Desktop | Mobile |
|----------|---------|--------|
| **white-space** | `pre` | `pre-wrap` |
| **Скролл** | Горизонтальный | Перенос строк |
| **Читаемость** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

### Таблицы

| Параметр | Desktop | Mobile |
|----------|---------|--------|
| **Обертка** | `.table-responsive` | `.table-responsive` |
| **Скролл** | Редко нужен | Почти всегда |
| **Страница растягивается** | ❌ Нет | ❌ Нет |

---

## 🎨 CSS: Медиа-запросы в action

**Полный код адаптивности:**

```css
/* ===========================
   Мобильная адаптивность
   =========================== */

/* Breadcrumbs */
@media (max-width: 768px) {
    .breadcrumbs-dynamic {
        font-size: 0.8rem;
        padding: 0.5rem 0;
    }
    
    /* Скрываем H3 */
    .breadcrumb-item.active {
        display: none;
    }
    
    .breadcrumb-separator:last-of-type {
        display: none;
    }
    
    /* Упрощаем dropdown */
    .breadcrumb-dropdown-menu {
        min-width: 200px;
        max-width: 90vw;
        max-height: 300px;
        font-size: 0.85rem;
    }
    
    .breadcrumb-dropdown-item {
        padding: 0.4rem 0.75rem;
    }
}

/* Code блоки */
@media (max-width: 768px) {
    .post-content pre {
        white-space: pre-wrap;
        word-wrap: break-word;
        overflow-x: auto;
    }
    
    .post-content pre code {
        white-space: pre-wrap;
        word-break: break-word;
    }
}
```

**Ключевой момент:**  
Все правила **только для мобильных** (≤768px), на десктопе работают стандартные стили.

---

## 🎓 Архитектурные решения

### ✅ Mobile-First vs Desktop-First

**Мы используем Desktop-First:**

```css
/* Базовые стили для desktop */
.breadcrumbs-dynamic {
    font-size: 0.9rem;
}

/* Переопределяем для mobile */
@media (max-width: 768px) {
    .breadcrumbs-dynamic {
        font-size: 0.8rem;
    }
}
```

**Альтернатива — Mobile-First:**

```css
/* Базовые стили для mobile */
.breadcrumbs-dynamic {
    font-size: 0.8rem;
}

/* Переопределяем для desktop */
@media (min-width: 769px) {
    .breadcrumbs-dynamic {
        font-size: 0.9rem;
    }
}
```

**Почему Desktop-First?**  
- Наша основная аудитория — разработчики (используют desktop)  
- Легче добавить упрощения, чем улучшения  
- Меньше переопределений CSS

---

### ✅ Точка перелома: 768px

**Почему именно 768px?**

| Устройство | Ширина экрана | Категория |
|------------|---------------|-----------|
| iPhone SE | 375px | Mobile |
| iPhone 14 | 390px | Mobile |
| iPad Mini | 768px | **Граница** |
| iPad | 810px | Tablet |
| Desktop | 1280px+ | Desktop |

**768px** — стандартная точка перелома между mobile и tablet в Bootstrap.

---

### ✅ Проверка идемпотентности

**TableProcessor** проверяет, не обернута ли таблица уже:

```python
if table.parent and table.parent.name == "div":
    parent_classes = table.parent.get("class", [])
    if "table-responsive" in parent_classes:
        continue  # Пропускаем
```

**Зачем:**  
- Можно безопасно запускать `create_posts` несколько раз  
- Не создается вложенность `<div><div><table>`  
- Легче отлаживать HTML

---

## 📈 Результаты

**Метрики:**

- **+80 строк CSS** (медиа-запросы)  
- **+15 строк Python** (TableProcessor обертка)  
- **3 компонента** адаптированы (breadcrumbs, code, tables)

**UX на мобильных:**

✅ Breadcrumbs занимают 1 строку вместо 2  
✅ Code блоки не требуют горизонтального скролла  
✅ Таблицы не растягивают страницу  
✅ Dropdown помещается на экране (max-width: 90vw)

**Технические выгоды:**

✅ Один CSS файл для всех устройств  
✅ Идемпотентная обработка таблиц  
✅ Легко добавить новые breakpoints (1024px, 1440px)

---

## 🔗 Что дальше?

Phase 2.7 завершена! Все модули реализованы и адаптированы для мобильных.

**Следующие фазы:**

- **Phase 2.8:** Поиск по постам с векторизацией  
- **Phase 2.9:** Комментарии с WebSockets  
- **Phase 3.0:** Админка с Unfold UI

---

**Последнее обновление:** 14 декабря 2025  
**Коммиты:** `9203464`, `878d880`
