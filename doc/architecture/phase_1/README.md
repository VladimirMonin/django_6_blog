# � Фаза 1: Базовый Django Blog

> От первого коммита до работающего блога с HTMX

**Период:** Декабрь 2025  
**Коммиты:** `f28d8ba` - `eb5995c`  
**Серий:** 9

Эта фаза охватывает создание базового блога с современным стеком: Django 6 beta, Bootstrap 5, django-components, HTMX. От пустого проекта до полноценного SSR-приложения с динамическими обновлениями без JavaScript-фреймворков.

---

## 🎯 Цели фазы

1. ✅ Настроить Django 6 с Poetry 2
2. ✅ Создать модель Post с авто-генерацией slug
3. ✅ Реализовать UI с Bootstrap 5
4. ✅ Внедрить Django 6 Template Partials
5. ✅ Создать компонентную архитектуру (django-components)
6. ✅ Интегрировать HTMX для динамических обновлений
7. ✅ Реализовать SEO-friendly пагинацию

---

## 📊 Статистика фазы

- **Коммитов:** 40+
- **Файлов изменено:** 150+
- **Строк кода:** +2000
- **Компонентов создано:** 4 (Button, PostCard, Alert, Paginator)
- **Серий документации:** 9

---

## 📖 Серии фазы

### [Серия 01: Фундамент проекта — Django 6, Poetry и структура](01_project_foundation.md)

**Коммиты:** `f28d8ba`, `1bcef2f`

Первый шаг — создание базовой структуры проекта. Почему Django 6 beta? Зачем Poetry вместо pip? Почему шаблоны и статика в корне, а не в приложениях? Разбираем архитектурные решения, которые станут фундаментом всего проекта.

**Ключевые темы:**

- Django 6 и его новые возможности (Template Partials)
- Управление зависимостями через Poetry 2
- Структура проекта: `config/` vs традиционный подход
- Централизованные `templates/` и `static/`

---

### [Серия 02: Базовая структура — маршруты, модель и первый hotfix](02_foundation_routes_data.md)

**Коммиты:** `ff56832`, `c28dd00`, `d69676c`, `c264d34`, `a6e9c00`, `eba10d4`, `4efb0b1`, `ba7c711`

Закладываем фундамент блога: URL-маршруты, модель Post, Django Admin и инструменты разработки. Встречаем классическую проблему с кириллицей в слагах и решаем её с помощью транслитерации. Создаём management команды для генерации тестовых данных и миграции слагов.

**Ключевые темы:**

- Двухуровневая маршрутизация Django (config/urls.py + blog/urls.py)
- Модель Post: минимальный набор полей для блога
- Авто-генерация slug: проблема с кириллицей и решение через transliterate
- Management команды: create_posts и regenerate_slugs с dry-run
- Django Admin: настройка для удобного управления постами

---

### [Серия 03: Шаблоны и стилизация — Bootstrap 5 и кастомный CSS](03_templates_and_styling.md)

**Коммиты:** `8f775d3`, `073a6fd`

Создаём полноценный UI блога с современным дизайном. Реализуем систему шаблонов Django с наследованием, подключаем Bootstrap 5 через CDN, создаём кастомные стили с CSS-переменными. Разбираем активное состояние навигации, hover-эффекты для карточек, улучшенную типографику для читаемости и flexbox-структуру для footer.

**Ключевые темы:**

- Архитектура шаблонов: base.html с блоками и наследование
- Bootstrap 5 через CDN: преимущества и подключение с integrity
- Реализация дизайн-концепции: белый фон, черные кнопки, желтые акценты
- Активная навигация через request.resolver_match.url_name
- CSS-переменные для управления цветами проекта
- Hover-эффекты: transform и box-shadow для карточек
- Улучшенная типографика: col-lg-8, font-size 1.1rem, line-height 1.8
- Flexbox для прижатия footer к низу страницы

---

### [Серия 04: Django 6 Template Partials — модульность без лишних файлов](04_template_partials.md)

**Коммит:** `dda7c01`

Внедряем Template Partials — киллер-фичу Django 6. Рефакторим шаблоны, выделяя переиспользуемые фрагменты прямо внутри файлов. Готовим инфраструктуру для HTMX: теперь можно возвращать только части страницы без создания отдельных файлов.

**Ключевые темы:**

- Template Partials: что это и зачем нужны
- Синтаксис `{% partialdef %}` и `{% partial %}`
- Рефакторинг post_list.html: partials для карточек и пустого состояния
- Рефакторинг post_detail.html: разделение на header, content, meta
- Рефакторинг base.html: переиспользуемая навигация
- Подготовка к HTMX: возврат partial через `template.html#partial-name`
- Сравнение: до и после (количество файлов, читаемость)
- Best practices для работы с partials

---

### [Серия 05: Django Components — модульная архитектура UI](05_django_components.md)

**Коммит:** `a235121`

Эволюция от Template Partials к полноценным компонентам. Интегрируем стороннюю библиотеку django-components, создавая инкапсулированные UI-элементы с собственной логикой, стилями и автоматической загрузкой ресурсов. Каждый компонент — это три файла (.py, .html, .css), работающие как единое целое.

**Ключевые темы:**

- django-components: философия и возможности сторонней библиотеки
- Установка через Poetry с Python version constraints (<4.0)
- Настройка settings.py: INSTALLED_APPS, builtins, STATICFILES_FINDERS
- Архитектура компонента: Python-логика, HTML-шаблон, изолированные стили
- Button компонент: универсальная кнопка с props (text, url, style, icon, active)
- PostCard компонент: композиция (использует Button внутри)
- Alert компонент: типизированные уведомления с иконками
- Автозагрузка CSS через {% component_css_dependencies %}
- Переход от partials к components: от 60 строк к 20
- Композиция компонентов: PostCard использует Button
- Подготовка к HTMX: компоненты как endpoint'ы для AJAX

---

## 🎬 Фаза 1.8: HTMX — интерактивность без JavaScript-фреймворков

### [Серия 06: HTMX интеграция — SPA без JavaScript-фреймворков](06_htmx_integration.md)

**Коммиты:** `f0f88f6`, `aecb561`, `a0454e5`, `dc52e10`, `2edc84e`

Добавляем динамические взаимодействия с помощью HTMX — библиотеки, которая даёт AJAX прямо из HTML-атрибутов. Никакого React/Vue, никакого написания JavaScript. Сервер отдаёт готовый HTML, HTMX обновляет части страницы. Реализуем поиск с debouncing, индикаторы загрузки, CSS-анимации для плавных переходов.

**Ключевые темы:**

- HTMX: философия "HTML over the wire"
- Установка django-htmx 1.27.0 и настройка middleware
- CSRF защита для HTMX через meta-тег и event listener
- Базовые атрибуты: hx-get, hx-trigger, hx-target, hx-swap
- Определение HTMX-запросов через request.htmx
- Partial templates vs Full page templates
- CSS анимации для HTMX: fadeInUp, htmx-indicator
- Debouncing для поиска: delay:500ms
- Graceful degradation: работает без JavaScript (SEO)

---

### [Серия 07: HTMX + Components + Partials — архитектура взаимодействия](07_htmx_components_partials.md)

**Коммиты:** `9a138e5`, `f5575b5`, `fa048e6`

Три технологии работают вместе: Django Templates (полные страницы), Django Components (переиспользуемые блоки) и Partial Templates (HTMX-ответы). Реализуем три режима рендеринга в одном view, используем Out-of-Band swaps для одновременного обновления разных частей страницы. Создаём архитектуру, где компоненты живут в partials, а partials возвращаются по HTMX.

**Ключевые темы:**

- Три слоя абстракции: Templates, Components, Partials
- Когда использовать каждую технологию
- Файловая структура: components/ vs templates/
- Поток данных при HTMX-запросе (sequence diagram)
- Out-of-Band swaps: hx-swap-oob для одновременных обновлений
- Три режима рендеринга: full page, HTMX normal, HTMX load_more
- Сценарий 1: Поиск с заменой контента (innerHTML)
- Сценарий 2: "Загрузить ещё" с добавлением (beforeend + oob)
- Важные нюансы: уникальные id для oob, load_more=='true'

---

### [Серия 08: Paginator — компонент с двумя режимами работы](08_paginator_component.md)

**Коммиты:** `9a138e5`, `ac6bd67`

Детальный разбор Paginator компонента: традиционная пагинация (кнопки 1, 2, 3) + прогрессивная загрузка ("Загрузить ещё"). SEO-friendly ссылки работают без JavaScript, HTMX добавляет интерактивность. Умная логика показывает релевантные страницы (максимум 5 кнопок), Out-of-Band swap обновляет пагинатор при добавлении постов.

**Ключевые темы:**

- Зачем два режима: быстрый переход vs бесконечный скролл
- Python логика: умная пагинация (макс 5 кнопок)
- Формирование URL с search параметрами
- SEO-friendly: href + hx-get на одном элементе
- Out-of-Band swap для автозамены пагинатора
- Кнопки страниц: hx-swap="innerHTML", hx-push-url="true"
- Кнопка "Загрузить ещё": hx-swap="beforeend", load_more=true
- CSS: желтая активная страница, hover эффекты
- Адаптивность: скрывает текст на мобильных
- Исправление бага: total_pages - 2 через фильтр add:"-2"

---

### [Серия 09: Баги с дефисами — Django Template Syntax](09_template_syntax_fixes.md)

**Коммиты:** `ac6bd67`, `9a138e5`

Три ошибки TemplateSyntaxError, которые стоили полутора часов отладки. Разбираем, почему Django не позволяет конкатенацию классов с дефисом (`bi-{{ icon }}`), арифметику в условиях (`total_pages - 2`) и как правильно размещать HTMX-атрибуты. Процесс отладки через grep, решения и best practices для предотвращения.

**Ключевые темы:**

- Баг #1: Конкатенация bi-{{ icon }} вызывает TemplateSyntaxError
- Решение: Формировать icon_class в Python
- Баг #2: Арифметика total_pages - 2 в {% if %}
- Решение: Фильтр add:"-2" или вычисление в Python
- Баг #3: HTMX атрибуты на корневых элементах
- Процесс отладки: traceback, grep поиск, пошаговое исправление
- Best practices: логика в Python, представление в шаблонах
- Чеклист проверки шаблонов перед коммитом
- Статистика багов: 1.5 часа на три ошибки

---

## 🔮 Следующая фаза

**Фаза 2: Markdown & Advanced Features** (в планах)

- Интеграция Markdown для контента
- Подсветка кода (Prism.js)
- Система комментариев с HTMX

---

## 🔗 Навигация

- [← Главный каталог](../README.md)
- [Планы фазы 1](../../plans/phase_1/)

---

**Последнее обновление:** 14 декабря 2025 г.
